import React, { Fragment } from 'react'

const SearchForm = ({params, onParamsChange}) => {
    return (
        <Fragment>
            <form>
                
            </form>
        </Fragment>
    )
}

export default SearchForm;
